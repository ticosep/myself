import cog from '../../../images/cog.png';

const Cogs = () => {
    return (
        <img
            className="w-[20%] h-[20%] float-left mr-4 object-contain animate-spin-slow"
            src={cog}
            alt="image_cog1"
        />
    );
};

export default Cogs;
